model Reviews {
    id         String   @id @default(uuid())
    user_id    String
    meal_id    String
    rating     Int
    comment    String
    created_at DateTime @default(now())

    @@index([user_id])
    @@index([meal_id])
    @@map("reviews")
}
