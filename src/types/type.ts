export enum UserRole {
  USER,
  PROVIDER,
  ADMIN,
}

export enum UserStatus {
    ACTICE,
    SUSPENDED
}

export enum OrderStatus {
    PLACED,
    PREPARING,
    READY,
    DELIVERED,
    CANCELLED
}